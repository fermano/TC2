# Northstar RC82 readout note

The lane count smoke at 18:27 BRT is green. The readout does not compare the badge string for `case-440`.

Support has one note that says the issue is probably fixed because PR validation passed; release has another note saying the explicit `needsReview=false` payload was not replayed through the release artifact.
