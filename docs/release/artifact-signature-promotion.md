# Artifact signature promotion gate

Release artifacts must report `signature_verified: true` before entering the promotion queue. Unsigned artifacts remain rejected and should be rebuilt from the approved release candidate.

The gate is active for the 2026-06-17 release checkpoint.
